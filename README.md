# 📌 Production Task Management System

Веб-приложение для управления задачами (production task / job tracking) с интерфейсом, сохранением данных и логикой обработки задач — реализовано с backend на Python и frontend-шаблонами. ([GitHub][1])

Проект позволяет:

* создавать, просматривать и управлять задачами;
* отслеживать статус выполнения;
* использовать простую веб-панель для взаимодействия с системой.

---

## 🧩 Основные возможности
```
✔️ CRUD-операции с задачами
✔️ Веб-интерфейс с шаблонами
✔️ Статические ресурсы (CSS/JS)
✔️ Серверная логика обработки задач
✔️ Подходит для расширения в production-окружении ([GitHub][1])
```
---

## 🧱 Технологии и стек

**Backend:**

* Python (основной язык проекта) ([GitHub][1])
* FastAPI (фреймворк)
* SQLalchemy (взаимодействие с БД)
* PostgresSQL (БД)
* Pydantic (валидация)
* Authx (аутентификация)

**Frontend:**

* HTML шаблоны
* CSS стили
* JavaScript взаимодействие с API

**Структура:**

````
app/            # Основной код приложения
templates/      # HTML-шаблоны
static/         # CSS/JS/изображения
````
---

## ⚙️ Установка и запуск

### 1) Клонирование репозитория

```bash
git clone https://github.com/SlayerLeet/Production_task_management_system.git
cd Production_task_management_system
```


### 2) Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate     # Linux/macOS
venv\Scripts\activate        # Windows
```

### 3) Установка зависимостей

```bash
pip install flask sqlalchemy 
```

### 4) Запуск сервера

Запусти приложение командой:

```bash
fastapi dev app/main.py        # или python run.py
```

После запуска доступ по адресу:

```
http://127.0.0.1:8000     # стандартный локальный URL
```

---

## 🧠 Использование

После старта сервера открой браузер и перейди по адресу, чтобы увидеть интерфейс системы управления задачами.

В веб-интерфейсе можно:

* добавлять задачи
* просматривать список задач
* редактировать/удалять задачи

---

## 🧪 Тестирование

Тестов в проекте сейчас нет — можно добавить:

---

## 📌 Автор

**SlayerLeet** — разработчик этого проекта и его репозитория. ([GitHub][1])

---

[1]: https://github.com/SlayerLeet/Production_task_management_system "GitHub - SlayerLeet/Production_task_management_system"
